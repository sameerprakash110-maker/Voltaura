# VOLTAURA backend + ML dependencies
# Install with:  pip install -r backend/requirements.txt

# --- API ---------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# --- persistence -------------------------------------------------------
SQLAlchemy>=2.0
# For PostgreSQL, uncomment and point VOLTAURA_DATABASE_URL at
# postgresql+psycopg://user:pass@host/db
# psycopg[binary]>=3.2

# --- ML / analytics ----------------------------------------------------
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
scipy>=1.13

# --- optional ----------------------------------------------------------
# Only needed if VOLTAURA_LLM_API_KEY is set for narrative enrichment.
# anthropic>=0.40
# Only needed if GEMINI_API_KEY is set for investigation reasoning.
google-genai>=1.0
